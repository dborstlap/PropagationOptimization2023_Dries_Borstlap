from tudatpy.io import save2txt
import os



# current_dir = os.path.dirname(__file__)
# output_path = current_dir + '/SimulationOutput/prop_' + str(0) + '/int_' + str(
#     0) + '/step_size_' + str(0) + '/'
#
# open(os.path.join(output_path, 'aaaaaaaaaaaaaaaaaaaaaaaa.dat'), mode='w').close()

# current_dir = os.path.dirname(__file__)
# output_path = current_dir + '/SimulationOutput/'
#
# propagator_index = 0
# integrator_index = 0
# step_size_index = 0
# name = 'prop_' + str(propagator_index) + '_int_' + str(integrator_index) + '_step_size_' + str(step_size_index)
#
#
# state_difference = dict()
# state_difference['title'] = name
# save2txt(state_difference, 'testing.dat', output_path)












